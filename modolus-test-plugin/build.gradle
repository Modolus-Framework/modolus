dependencies {
    compileOnly project(":modolus-core")
    annotationProcessor project(":modolus-processor")
}

tasks.compileJava {
    options.compilerArgs += [
            "-AprojectVersion=1.0.0"
    ]
}
